# 変更履歴

このファイルの書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に、
バージョン番号は [Semantic Versioning](https://semver.org/lang/ja/) に従う。

0.x 系のため、マイナー版の更新に破壊的変更を含むことがある。
API・DB スキーマの互換方針とアップグレード手順は
[docs/design/release-process.md](docs/design/release-process.md) を参照。

**0.7.0 が最初の公開リリース。** 0.1.0〜0.6.0 は公開前の開発マイルストーンで、
タグは打たれていない(Git 履歴から遡って再構成したもの)。末尾のリンクも
これらについてはタグではなくコミットの比較 URL を指している。

## [Unreleased]

## [0.7.0] - 2026-08-27

最初の公開リリース。シフト制(1ヶ月単位の変形労働時間制)、有給付与の3段フロー、
セルフホスト運用に必要な Tier 0〜2 の機能一式。

### Added

- **シフト制 / 1ヶ月単位の変形労働時間制**(`monthly_variable`)
  - エンジン: ①日 ②週 ③変形期間の3段判定。期間段は「期間末が属する月」に帰属し、
    月をまたぐ週は前月側を独立した全月パスで計算して前月の締めと整合させる
  - 予実乖離の警告5種(シフト未設定 / 遅刻 / 早退 / 計画外勤務 / 欠勤)
  - シフトパターン CRUD、週グリッドでのメンバー別割当、確定と変更履歴。
    確定時に法定休日(週1日 or 4週4日)が不足していれば拒否
  - 本人向けのシフト表(月カレンダー)、ダッシュボードの今日・明日のシフト
  - 月次画面に変形期間バー・所定列・乖離警告
  - 予実乖離の本人向け通知(カテゴリ `shift_variance`。管理者ダイジェストとは別枠)
  - 新権限 `shift.manage`
- **有給付与の3段フロー**: 基準日30日前に日次ジョブが付与予告を作成 → 管理者が承認・却下
  → 本人へ通知。出勤率の参考値(分母はシフト勤務日/暦日推定、分子は出勤日∪承認済み有給)を
  添え、8割未満は警告として示す
- **比例付与**(労基法39条3項 / 施行規則24条の3): 付与区分 `full` / `days4`〜`days1`。
  不明値は `full` に倒す(少なく付与する方向には倒さない)。年5日義務は日数判定のため変更不要
- **OIDC SSO ログイン**: code + PKCE(S256) + state + nonce。自動プロビジョニングはせず、
  招待式の原則どおりメールが一致する既存ユーザーのみログイン可。テナント単位設定
  (client secret は保存時暗号化・GET はマスク)、discovery/JWKS の10分キャッシュ。
  新権限 `tenant_settings.auth.manage`
- **PostgreSQL ダイアレクト**: `DATABASE_URL` で SQLite / PostgreSQL を選択。
  スキーマは sqlite-core が単一の正で、pg-core は実行時生成のミラー(手書きの二重管理なし)。
  クエリ層(約150関数)は1実装のまま両方言で動作
- **Cloudflare Workers + D1 の一次実装**(ビルドとテストのみ、デプロイ経路は未提供):
  `@kizami/db` をランタイム非依存 index と `/node` に分割、`apps/api/src/workers.ts` +
  `wrangler.jsonc`。D1 が `BEGIN`/`SAVEPOINT` を拒否するため書き込み系が未対応である点は
  該当箇所一覧つきで `docs/design/workers-d1.md` に文書化(v1.0 の残ギャップ)
- **多段承認(2段階)**: 種別ごとにオプトイン。既定1段で挙動不変。2段目は同権限の tenant
  スコープ保持者で1段目とは別人必須。申請は作成時点の段数で固定(グランドファザリング)。
  1段目承認は状態遷移のみで、打刻の supersede・有給消費・控除の打ち消しは最終承認まで不適用。
  新権限 `approval_flow.manage`
- **Web Push 通知**: VAPID を自前実装(RFC 8291/8292/8188 を WebCrypto で)。
  `web-push` npm は `node:crypto` 依存のため不採用とし、`@kizami/notify` のランタイム非依存を維持。
  鍵未設定時は fail-soft(機能ごと非表示)、404/410 は遅延剪定。`pnpm generate-vapid` を追加
- **権限の deny ルール**: 合成が「加算のみ」から「(grants の union) −(denies の union)」へ。
  deny はスコープなしの全面優先で、含意展開の前後2回適用(拒否対象が他権限の含意で復活しない)。
  セルフサービス3権限は deny 不可
- **認証系のレート制限**: sliding window(メモリ内)。ログインは ip+email 10/15分 と ip 30/15分、
  招待・リセットのトークン系は GET 含め ip 20/15分、Bearer(打刻 API キー)は ip 120/分。
  `TRUST_PROXY=false` で直接公開にも対応
- **マルチテナント運用の有効化**: テナント作成 CLI(`pnpm create-tenant`)、
  compose の `create-tenant` プロファイル、クロステナント分離の監査テスト14本
- **コアタイム**(フレックス、労基法32条の3): 遅刻 / 早退 / 不在の警告3種。
  法定休日・有給日・未来日は適用外。**集計には影響しない(警告のみ)**
- **給与ソフト向け CSV エクスポート**: `?format=generic|freee|mf`。
  freee は公式サンプル CSV とバイト検証済み。MF は固定列仕様が公開されていないため
  HH:MM 出力とし、要検証事項を `docs/design/payroll-export.md` に明記
- **オンボーディングツアー**: 依存追加なしの自前スポットライト(focus trap、Esc/矢印、
  reduced-motion、モバイル対応)。メンバー6歩 / 管理者 +4歩、複数ページにまたがって継続
- **監査ログの閲覧 API と画面**(読み取りのみ・カーソルページング)
- **他人の勤怠閲覧**: `GET /attendance/monthly?userId=`(`attendance.record.view` + スコープ。
  スコープ外は存在推測を避けるため404)、`GET /attendance/members`、月次画面のメンバー切替
- **Tier 0(製品として成立させるための穴埋め)**
  - 管理者発行のパスワードリセット(一度きりリンク、TTL 24h、使用時に全セッション失効)。
    SMTP がなくてもリンク手渡しで運用できる
  - 退職処理(無効化 / 再有効化)。無効化は全セッション・pending 招待・未使用リセットを
    1トランザクションで失効。最後の権限管理者は「有効なユーザー」で数えて保護
  - 労働時間制の個別割当(`GET`/`POST /members/:id/work-policy`)。
    固定時間制が製品経路で使えるようになった(従来はシード直挿しのみ)
  - 承認依頼通知: 申請作成時にスコープ内の承認権限保持者へ。共有チャネルの文面には
    理由・時刻を含めない(プライバシー原則)
- **Helm チャート**(`deploy/helm/kizami`)と配布用 Docker Compose(`deploy/compose`)
- **kizami.dev / docs.kizami.dev / demo.kizami.dev**: 校正紙モチーフのプロダクトサイト、
  VitePress ドキュメントの配信、毎晩リセットされる公開デモ環境
- CONTRIBUTING / SECURITY と README の刷新(OSS 公開準備)

### Changed

- 月次レスポンスの契約を再編(**破壊的**): `figures{source: live|snapshot, totals, flexBalance,
  fixedBreakdown, allowanceTotals, original?}` / `closing{closed, amended}` / `user` の構成へ。
  `fixedBreakdown` を Web に初めて表示。`apps/mcp` も追従
- 制度ガイド(help-content)を4言語化(23キー × en/ko/zh)。ロケール対応辞書と ja 明示フォールバック、
  参考訳である旨の注記
- システムプリセットの権限をカタログ追加分だけ同期するシード経路を追加。
  従来は初回シードがユーザー既存時に全スキップするため、権限カタログに項目を足しても
  本番のシステムプリセットに伝播せず、固定原則で UI からも編集できなかった
- ドキュメントの配信を `docs.kizami.dev` に一本化(GitHub Pages workflow を削除)

### Fixed

- `POST /shifts/plans` が他テナントの `userId` で 201 を返していた
  (`scope=tenant` の `resolveAccessibleUserIds` が `'all'` を返し素通り)
- `POST /api-keys` が他テナントの `userId` でも作成できていた
- `corrections` / `leave` / `waivers` の `?userId=` が他テナントで 200 `[]` を返していた(404 に統一)
- `monthly_variable` で有給の分数が解決できず `GET /leave/balance` が 500 になっていた
  (確定シフトの所定 → 無ければ版の `standard_day_minutes` で解決)
- 勤怠設定の work-policy POST が `kind` 未送信で 400 になっていた
- CI の PostgreSQL スモークがテーブル数を固定値で持っており、スキーマ追加のたびに落ちていた
  (`migrations-pg` から動的導出に変更)
- JSDoc の `[a, b](注)` 表記が typedoc → VitePress で dead link 扱いになり CI が落ちる問題

### Security

- `PATCH /presets` に last-admin ガードを新設。deny を1つ足すだけで、割当を触らずに
  権限管理者を全滅させられる状態だった
- 429 応答をメールアドレスの実在で分岐させない(ユーザー列挙防御の維持をテストで固定)

## [0.6.0] - 2026-08-23

多言語 UI、手当対象時間、5方向レビューの指摘修正、内部の Temporal 化。

### Added

- **多言語 UI(日 / 英 / 韓 / 中簡体)**: i18n 辞書を4ロケールに分割し、型を `ja` から導出して
  他言語のキー過不足をコンパイルエラーにする(翻訳漏れの構造的防止)。`messages` を Proxy 経由に
  したため既存コンポーネントは無改修で追従。法令用語は労基法の公式英訳に準拠し、
  日本の制度を現地制度に置き換えない
- **手当対象時間**: 特定日(毎年 `--MM-DD` / 固定日)・曜日・時間帯(日跨ぎ可)の AND 条件と
  実労働の重なりを勤怠日別に算出。法定深夜とは独立(重複可)、有給日は対象外、法定休日労働は対象。
  **金額は計算しない**原則は維持。締めスナップショットに `allowance:<id>` 区分で保存し、
  CSV に `allowance_<name>` 列を追加
- 月次の日付セルに承認済み休暇のバッジ(全休 / 半休 / 時間単位 + 分数)
- **Storybook 8 によるデザインカタログ**: 実 CSS + 実コンポーネントを読み込む
  「生きたスタイルリファレンス」方式(部品抽出のリファクタはしない)。
  ツールバーでテーマ(light/dark)× ロケール(ja/en/ko/zh)を切り替えられる
- `GET /me/effective-permissions`(認可ミドルウェアの解決結果をそのまま返す)

### Changed

- **PC 幅の統一**: 認証後の全ページの `main` を 72rem に、フォーム・読み物は内側ブロックを
  44rem で左寄せ制約。見出し・設定ナビ・戻りリンクの左端が全ページで一致する
- **承認モデルを権限ベースに統一**(暫定の本人承認のみから): `corrections` / `leave` の
  approve/reject に `attendance.correction.approve` / `leave.request.approve` + スコープ検証。
  シード済み権限のデッド状態を解消し、管理者・マネージャーが他人の申請を承認できるように
- Web 側の実効権限の推定(10本プローブ / データ形状推定 / プリセット名再計算の3方式)を全廃し、
  `GET /me/effective-permissions` に一本化
- **内部の Temporal 化**(第4〜5波): エンジンの手書き暦アルゴリズムと apps/シードの日時処理を
  `Temporal.PlainDate` ベースへ。公開シグネチャ(エポック分・`YYYY-MM-DD`)は不変。
  ローダーは「ネイティブ優先・無ければ temporal-polyfill」。時間帯重なり計算は意図的に整数演算のまま
- 不正な日付文字列(`2024-02-30` 等)を黙ってロールオーバー / 1970 フォールバックしていたのを
  `RangeError` に変更(実呼び出し経路は全て検証済み入力のため実質バグ修正)
- `routes/settings.ts`(1430行・22ルート)を11ファイルに、`MonthlyView.tsx`(960行)を
  合成のみの133行 + 7部品に分割(挙動不変。月次系キャプチャ12枚のピクセル一致で確認)

### Fixed

- **自動控除の不感帯を解消**(Critical): 実効控除 `min(控除分数, gross − 閾値)` の最大を採用する方式へ。
  単一階層ルールで控除ゼロ + 誤警告になる構造を修正(法定2段ルールの挙動は全ケース不変)
- `member.invite` で `departmentId` を省略するとスコープ検証を素通りできた
- 招待メンバーにテナント既定の work policy が割り当てられず、有給・月次が 500 になっていた
- `messages` がモジュールレベル `const` で日本語に凍結され、言語切替に追従しないバグ(全数調査で4件)
- 条文引用の誤り: プライバシー通知の「個情法17条3項」→ 21条3項、「9人以下」→「10人未満」

## [0.5.0] - 2026-08-23

固定時間制、休憩の自動控除と打ち消し申請、招待式の登録フロー。

### Added

- **固定時間制(通常の労働時間制)**: `WorkSystem` を `flex` / `fixed` の判別可能ユニオンに。
  日次(1日8h超)→ 週次(週法定超)の二段判定で、日次控除後の値で週次を判定して二重計上を防ぐ。
  法定内残業(所定超〜8h)を独立区分として持つ
- **休憩不足の検知**(労基法34条、6h超45分 / 8h超60分): 勤務区間単位で判定し、
  日界をまたぐ夜勤を勤怠日で割ることによる見落としを防ぐ
- **休憩の自動控除と打ち消し申請**: `BreakRule` を `punch` / `auto` / `both` の判別可能ユニオンに。
  控除は勤務区間の末尾から削り、打刻由来と自動控除を分離して保持。
  「実際には取れなかった」ことを本人が申告して打ち消せる申請フロー(`auto_break_waivers`)を
  機能の一部として提供する(推定値を `punch_events` に混ぜない設計判断)
- **休憩不足検知のチェーン化**: 同一勤怠日の区間群を合算判定し、区間間の空白(中抜け)を
  休憩相当として算入。末尾の空白(帰宅)は数えない。日またぎの空白は勤務間インターバルとして峻別
- **招待式の登録フロー**: `invitations`(トークンは SHA-256 保存)、招待の発行 / 再発行 / 取り消し、
  公開の受諾エンドポイント(パスワード設定 → 即ログイン)、受諾ページ `/invite/[token]`
- 月次一覧に勤務列(出勤 → 退勤)。1200px 以上では出勤・退勤を独立列に。
  日界またぎは「翌」、受け側に「(前日から)」、未退勤は「—」、中抜けは全区間表示
- ヘッダーのロゴ横にテナント名(社名)を表示
- `week_start_weekday`(週の起算曜日)設定

### Changed

- `EngineOutput.flexBalance` を nullable 化し `workSystem` を追加。
  時間外列は固定時間制のみ表示(フレックスに日別の法定時間外は法的に存在しないため)。
  フレックス収支バーは固定時間制では36協定 月45h の進捗バーに置き換え
- 締めスナップショットに `fixedWithinScheduled` / `fixedExtraWithinStatutory` 区分を追加
  (所定内と法定内残業の区別は給与計算に必要なので、締めた月こそ残す)
- 警告の Y マークを廃止し行背景色に(文言が非色覚的手掛かりを既に担っているため)
- 打刻の無い日は `0:00` ではなく空セル表示

### Fixed

- 同一メールが複数テナントにあるときのログインが `LIMIT 1` の行順まかせだった。
  パスワード一致が複数なら 409 `multiple_tenants` でテナント一覧を返し(検証通過者にのみ社名開示)、
  `tenantId` 指定で再ログインする形に
- `buildSettingsTimeline` が対象期間より前の work policy 変更を無視していた
  (所定労働時間の変更が有給換算に反映されない等、賃金計算に影響する既存バグ)
- 自動控除のルール採否を厳密比較(`>`)で判定していたため、拘束9時間 + 法定2段ルールで
  60分が棄却され、最も標準的な 9:00-18:00 勤務が毎日休憩不足警告になっていた(`≥` に修正)
- 月次テーブルの警告文言が折り返されないバグ。`td` の `white-space: nowrap` が
  詳細度で警告用の `normal` に勝っており、折り返し指定は最初から一度も効いていなかった
- `uuidv7` に同一ミリ秒内の単調性カウンタを追加。`closing_events` の世代解決が
  close/amend 同時刻で確率的に逆転する地雷を修正
- 招待の再発行で「最新の招待」が行順まかせになるバグ(id タイブレークで修正)
- モバイルカードで出勤・退勤列が重複表示される詳細度バグ

## [0.4.0] - 2026-08-23

外部クライアントからの打刻(公開 API キー・MCP・Slack・PWA/GPS)、ダークテーマ、
ブランドアイデンティティ。

### Added

- **公開打刻 API とスコープ付き API キー**: 32バイト乱数を `kzm_` プレフィクス付きで発行し、
  DB には SHA-256 のみ保存。平文は発行時に1度だけ表示。スコープは `punch` / `read` のみで、
  許可リストにないエンドポイントは既定 403。設定変更・承認・締めは API キーでは一切できない
  (API キーには空の権限マップを与えるため `requirePermission` は必ず落ちる = fail-closed)。
  `/api-keys` 自体を許可リストから外し、漏洩したキーが別のキーを発行・失効できないようにする
- **MCP サーバー**(`apps/mcp`): 公開打刻 API の上に薄く実装した別プロセスで DB には触らない。
  API キーのスコープがそのまま MCP の権限になる。ツールは punch / get_status /
  get_monthly_summary / get_leave_balance / list_corrections の5つ。
  **修正申請を作成するツールは提供しない**(「なぜ直すか」の判断を AI が代筆すべきではない)
- **Slack スラッシュコマンド打刻**: `/punch in|out|break|back|status|link`。
  Signing Secret だけで動き始められる(Bot Token 不要)。署名検証は WebCrypto の HMAC-SHA256 +
  タイミング安全比較 + ±5分の時刻窓で、これが認証の代わりになる。応答は必ず ephemeral。
  紐付けは `/punch link` の15分ワンタイムトークンによる明示連携のみ
- **PWA + GPS 打刻**: manifest / Service Worker / アイコン。SW は打刻 API を一切傍受しない。
  オフラインでは打刻できないことを明示(キューイングは実装しない — 実際の打刻時刻と記録時刻が
  ずれて1分単位の正確性という前提が崩れるため)。GPS はテナントで有効なときだけ取得し、
  記録される旨・理由・保持期間を常時表示。許可されなくても打刻は止めない
- **ダークテーマ**: OS 設定 + 明示選択の3状態。地は純黒にせず `#141618`、テキストは `#F2F2EE`。
  CMYK の原色はそのまま使い、ink 変種は明るい変種に差し替えて全て AA(4.5:1)以上を確保
- **ブランドアイデンティティ**: トンボ(印刷の見当合わせマーク)に時計の針を重ねたロゴ。
  SVG 1ソースから全サイズを生成(`scripts/generate-icons.mjs`)。文字ロゴを明朝から
  幾何学サンセリフ(Jost)へ。状態スタンプの書体(Shippori Antique B1)は活版の趣として維持
- **ダッシュボード(`/`)と モバイルの下部タブバー**: 打刻画面は `/punch` へ移したが、
  打刻を1タップ増やさないようダッシュボード最上部に状態と打刻ボタンを置く。
  「要対応」は該当があるときだけ出す。新しい API は作らず既存5本の並行取得で構成
- **通知一覧(`/notifications`)とオンボーディング**: 状態 / 種別での絞り込み、
  個別・一括の既読化(一覧を開いただけでは既読にしない)。オンボーディングはモーダルで
  強制せず未完了の項目だけを出し、判定は既存 API から導出する(専用テーブルを作らない)
- **自動スクリーンショットパイプライン**(`pnpm screenshots`): 一時 API/Web の起動 →
  デモデータ投入 → Playwright で全画面をライト/ダーク × デスクトップ/モバイル撮影 →
  一覧 HTML 生成 → 後片付け、までコマンド一発

### Changed

- `GET /settings/attendance/capabilities` を追加(認証のみ)。打刻する従業員は GPS の有無を
  知る必要があるが、設定 API は管理権限を要するため

### Fixed

- **通知の宛先がテナント単位でしか持てず、全従業員の通知が1つの共有 Webhook に集約されて
  他人の勤怠情報が見えていた**(設計の誤り)。`user_notification_settings` を新設し、
  本人宛の通知はテナント共有 Webhook を一切使わない `buildPersonalChannels` に切り替え。
  既定はアプリ内のみ ON
- `node.ts` が `/` と `/api/*` の両方で配信するため、API キーのスコープ判定が
  `/api/punches` で効かず 403 になっていた
- ダークテーマ実装中に発見した設計の綻び: 原色塗りの上のテキストがテーマ反転で
  コントラストを壊す(シアン塗りに白文字で 2.58:1 等)。塗りとテキストの役割を分離し、
  ハードコードされていた色 約70箇所をトークン / `color-mix` に置換

## [0.3.0] - 2026-08-22

月次締めと CSV エクスポート、有給休暇、法令ルールの版管理、コンテキストヘルプ。

### Added

- **月次締めと締め後修正(amend)**: `closing_events`(追記専用)から締め状態を導出し、
  状態テーブルは持たない。締め時に全ユーザーのスナップショットを保存し、締め済み月の表示・
  エクスポートは常にそこから読む(制度変更の遡及から二重に保護)。
  締め済み月でも**申請は作成できる**が、承認だけを `closing.unlock` 権限保持者に限定し、
  月を開けずに1件だけ反映する。スナップショットは世代管理され、当初の確定値は残るため
  CSV の `?compare=original` で差分列を出せる(給与の差額調整用)
- **CSV エクスポート**(UTF-8 BOM + CRLF、区分別時間数とフレックス収支)
- **有給休暇**(`@kizami/leave`): 法定付与テーブル・基準日方式・時効2年・FIFO 消化を純関数で実装。
  全休 / 半休 / 時間単位を分単位に統一しつつ法的差異を実装 — 半休は年5日義務に 0.5 日カウント可、
  時間単位はカウント不可かつ年5日分が上限(所定を1時間単位に切り上げて算出、繰越分も同枠)。
  失効分の積立休暇への振替(冪等・上限で切り捨て)。承認済み有給を月次集計のフレックス枠まで結線
- **法令ルールの版管理**(`@kizami/law`): 週法定労働時間・深夜帯・36協定閾値・有給付与テーブル等を
  適用開始日付きの版として集約し、根拠条文をデータとして保持。企業規模で施行日が異なる改正
  (60時間超割増、36協定上限)に対応し、将来の版を先に書いておけば施行日に自動反映される。
  総枠は日割り按分、閾値は期間開始日の版、有給の付与日数・時効は付与日の版で確定
- **36協定アラート完全版**: 年360h(実績・見込み)と特別条項の4上限(月100h未満・
  複数月平均80h・年720h・月45h超は年6回)。月100h/平均80h は時間外+休日労働、
  年上限は時間外のみという法令上の対象範囲の違いも反映
- **有給の期限通知**: 失効間近(60/30/7日前)と年5日義務の期限接近(90/30日前)。
  半休は 0.5 日として数えるが時間単位は含められないという法的注意を文面に必ず入れる。
  導入直後に過ぎた段階がまとめて飛ぶノイズを避け、緩い段階は当日一致でのみ発火
- **コンテキストヘルプ**(`@kizami/help-content`): 説明の出どころを必ず区別する。
  frontmatter に `origin`(law / product / company)・`basis`(法令なら根拠条文必須)・
  `audience` を持ち、「法令 · 労働基準法39条7項」のように根拠を添えたバッジを表示。
  型付き辞書を生成し、存在しないキーの参照はコンパイルエラーになる
- **自社ルールの併記**: `help_overrides`(テナント × ヘルプキー)と就業規則 URL。
  **読むのは認証だけ**(従業員も自社の規定を知る必要がある)、書き込みは権限が必要。
  `docs-local/` オーバーレイで VitePress に「社内規定」セクションを追加(gitignore 済みで
  フォーク不要・アップグレードと衝突しない)。これで法令 / KIZAMI の仕様 / 自社の規定 の3層が揃う
- **個人情報の雛形**(`@kizami/privacy-template`): テナント設定を入力に文面を生成する純関数。
  GPS が無効なら位置情報の項を出さず、保持期間は設定値を反映。従業員向けプライバシー通知と
  社内利用規約の2種で、両方の冒頭に「雛形であり法的助言ではない」の注記を必須化
- **テナント設定の UI からの編集**(effective-dated): `/settings/attendance` と
  `/settings/work-policy`。**常に新しい版を追加する**(既存の版は UPDATE しない)。
  過去日からの適用は 409、同一日の重複も 409。GPS 有効化には従業員への明示義務の警告を出す
- **テナントプロファイル**(中小企業 / 特例措置対象事業場 / 特別条項)と適用予定の法改正の表示
- **TypeDoc による API リファレンス**: 7パッケージを Markdown 生成して VitePress に統合。
  `invalidLink` / `notExported` をエラー扱いにする検証を導入し、実際に `@kizami/db` の
  型漏れ(非公開スキーマ名前空間の露出)を検出・修正

### Changed

- エンジンの `paidLeaveDays`(日付配列)を `paidLeave({date, minutes} 配列)`に変更し、
  全休・半休・時間単位を同一の仕組みで扱う
- `EngineInput` に `lawTimeline` を追加。週法定労働時間・深夜帯・60時間超の閾値を
  ハードコードから法令版に置換(60時間超区分は施行前は常に 0)
- 権限カタログのレビュー論点10件を精査。`department.manage` を危険フラグに格上げ
  (スコープの実判定が入り、部署の移動が「誰が誰を閲覧・承認できるか」を変えるようになったため)

### Fixed

- **締めが有給を無視していた**: 締め処理・打刻忘れリマインド・36協定アラートが共有していた
  月次計算が `paidLeave` を空配列固定で呼んでおり、確定値から有給が欠落していた
  (時間外の見込みも過大になっていた)。「締める前の集計と締めたスナップショットが一致する」
  回帰テストを追加
- 従業員が自社の休暇制度を読めず、申請フォームの選択肢(時間単位・半休の可否)が
  正しく出ていなかった(認証だけで読める `GET /leave/capabilities` を追加)
- `buildSettingsTimeline` がテナント設定を2版以上持つと例外を投げていた

## [0.2.0] - 2026-08-22

修正申請フロー、通知基盤、権限エンジン、部署と権限プリセットの管理。

### Added

- **修正申請フロー**: `correction_requests`(訂正 / 追加 / 取消の3ケースを単一モデルで表現)。
  承認は1トランザクションで `punch_events` 反映 + 状態更新 + 監査ログ
  (追記専用モデルのまま — 訂正は `supersedes` 付き新イベント、取消は void イベント)。
  二重 supersede は 409 `already_superseded`、楽観ロック失敗は 409 `not_pending`。
  自己承認は許可しつつ監査ログに `selfApproved` を記録(単独運用での滞留回避)
- **通知基盤**(`@kizami/notify`): チャネル抽象(Webhook 実装、SMTP は送信関数注入型)。
  ランタイム非依存で並行送信・部分失敗を許容。`notifications` テーブルは UNIQUE で
  同一日の重複通知を防止
- **打刻忘れリマインド**: DB の定期スキャンでエンジンの `missing_clock_out` 警告のうち
  日界を過ぎた日を検知する(キューの永続性に依存しない自己修復設計)。
  worker は BullMQ + Valkey の定期実行で、ジョブ本体は純関数に分離して将来の
  Cloudflare Cron 移行に備える
- **36協定の時間外アラート(月45h)**: 実績到達と見込みの2種。月単位で1通に絞る
- **権限エンジン**(`@kizami/authz`): プリセット合算(広いスコープ優先)、操作 → 閲覧の含意展開、
  セルフサービス権限は常に付与。`PERMISSION_CATALOG`(30項目)をコード化し、
  `impliesView` は既存の `IMPLIED_VIEW_PERMISSIONS` から導出して二重管理を避ける
- **部署ツリーと権限プリセットの管理 API / UI**: 部署 CRUD(循環参照 400・非空削除 409)、
  メンバー一覧と所属変更、プリセット CRUD と割当。固定原則を保存前の仮実効権限で検証
  (自己昇格 409 / 自己降格 409 / 最後の権限管理者 409 / system プリセット編集 409)。
  プリセット編集画面は30項目を5カテゴリに整理し、説明文・スコープ選択・危険表示・
  「この権限には◯◯の閲覧が含まれます」を明示。メンバー行を開くと実効権限ビューを即時表示
- **テナント通知設定**と通知ベル(未読バッジ、60秒ポーリング・非表示時は停止)。
  一覧は明示操作でのみ既読化し、打刻忘れ通知から対象日の修正申請フォームへ直接遷移できる

### Changed

- `authMiddleware` がリクエストごとに実効権限を1回だけ評価してキャッシュする
- Valkey を独立 Service から同一 Pod 内サイドカーに変更(k3s でノードをまたぐ
  クラスタ DNS が機能せず worker が到達できなかったため。キューは起動トリガーでしかなく
  正しさは DB の定期スキャンが担保するため実害はない)

### Fixed

- **スコープが実リソースに適用されておらず、配下スコープしか持たないユーザーが
  テナント全体を操作できていた**。判定を `lib/scope.ts` に集約し、members / departments の
  一覧・作成・更新・削除・プリセット割当に適用。部署の親変更も移動先が自スコープ内であることを検証
- フレックスの見込み時間外が永久に 0 のままだった。フレックスの時間外は月の総枠との差なので
  月中の `totals.overtime` はほぼ常に 0 であり、それを日割りしても意味がなかった。
  実労働を月末まで伸ばして総枠と比較する式に変更
- `@kizami/authz` 追加時の COPY 漏れで本番の api コンテナが `ERR_MODULE_NOT_FOUND` で
  クラッシュした。個別列挙をやめ `packages/` をまとめてコピーする形に

### Security

- **通知の秘密情報を保存時暗号化**(`@kizami/crypto`): AES-256-GCM を WebCrypto で実装
  (`node:crypto` 不使用、Node/workerd 両対応)。形式は `enc:v1:<iv>:<ct>` でバージョン埋め込み。
  鍵は `KIZAMI_ENCRYPTION_KEY`(base64 32バイト)で、**未設定時は平文で保存せず
  503 `encryption_unavailable` で拒否**する。復号失敗は例外にせず該当チャネルだけ無効化。
  `enc:` が無い既存値は平文として読める(後方互換)

## [0.1.0] - 2026-08-21

集計エンジン・DB・API・打刻 UI の最小構成。フレックスタイム制(1ヶ月清算)。

### Added

- pnpm モノレポの雛形(engine / api / web / docs)。Node 26、TypeScript 7、Vitest 4、AGPL-3.0
- **設計の確定**: 打刻は追記専用(`punch_events` 不可変)、集計は常時再計算 + 締めスナップショット、
  制度・設定は effective-dated(過去期間への遡及影響を構造的に防止)。
  権限カタログ30項目・内部 CRUD 対応表・標準プリセット3種の割当表(危険フラグ12件)
- **集計エンジン**(`packages/engine`): 打刻解釈ステートマシン、日界配賦、深夜帯計算、
  フレックス月枠集計。保守的な不正打刻列の解釈ルール(未完区間は集計除外、矛盾打刻は無効化、
  いずれも警告付き)。日付演算は独自実装(civil calendar)で `Date` 非依存・純関数を維持。
  ゴールデンケース6本(空月 / 通常週 / 日跨ぎ深夜 / 退勤なし / 法定休日 / 不正列)
- **DB 層**(`packages/db`、Drizzle / SQLite / libSQL): 14テーブル、有効打刻の解決(`NOT EXISTS`)、
  設定タイムライン取得クエリ、drizzle-kit 初期マイグレーション
- **API**(`apps/api`、Hono): `POST /punches`(証跡メタ付き・未来打刻拒否)、
  `GET /attendance/status`、`GET /attendance/monthly`(有効打刻 + 設定タイムライン → エンジン計算)、
  `GET /punches`。`createApp({db})` ファクトリでランタイム非依存を維持。冪等シード CLI
- **打刻 UI**(`apps/web`、Waku): 状態スタンプ(CMYK 版ズレ → 見当合わせの刻印演出、
  reduced-motion 対応)、インキパッド打刻ボタン、今日のトンボ列。月次は K 主体のテーブルに
  フレックス収支の C 濃度バー・区分チップ・警告表示。デザイントークンは
  `docs/design/ui-direction.md` と一致
- **デプロイ資材**: multi-stage Dockerfile(api は非 root 1001 / `/data` ボリューム、
  web は `WAKU_PUBLIC_API_URL=/api` 焼き込み)、GHCR への amd64 + arm64 マルチアーチ push、
  k8s マニフェスト一式
- CI(Node 26 + pnpm、typecheck / test)

### Security

- セッショントークンを UUIDv7 から32バイト乱数に変更し、DB には SHA-256 ハッシュのみ保存
  (DB を読めてもセッション乗っ取りは不可)
- Secure Cookie を Node 起動時は既定 ON に(`COOKIE_SECURE=false` で明示無効化)
- ユーザー列挙対策: メール不存在時もダミーハッシュ検証を実行して応答タイミングを均一化
- 認証は PBKDF2-SHA256(600k, WebCrypto)+ セッション Cookie(30日)

### Fixed

- セッション Cookie に `Max-Age` が無くブラウザセッション限りになっていたため、
  DB セッションが30日有効でもブラウザを閉じるとログアウトしていた。
  併せて残り期限が TTL の半分を切ったアクセスで DB 期限と Cookie を延長する

<!--
  リンク参照。0.1.0〜0.6.0 は公開前の開発マイルストーンでタグが存在しないため、
  タグ名ではなくマイルストーン末尾のコミット SHA で範囲を示している。
-->

[Unreleased]: https://github.com/sasagar/kizami/compare/v0.7.0...HEAD
[0.7.0]: https://github.com/sasagar/kizami/compare/ece25ba...v0.7.0
[0.6.0]: https://github.com/sasagar/kizami/compare/2599955...ece25ba
[0.5.0]: https://github.com/sasagar/kizami/compare/8845be9...2599955
[0.4.0]: https://github.com/sasagar/kizami/compare/6c5b9af...8845be9
[0.3.0]: https://github.com/sasagar/kizami/compare/a4333bb...6c5b9af
[0.2.0]: https://github.com/sasagar/kizami/compare/01e2e0d...a4333bb
[0.1.0]: https://github.com/sasagar/kizami/commits/01e2e0d
