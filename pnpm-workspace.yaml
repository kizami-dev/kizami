packages:
  - apps/*
  - packages/*
  - docs

# workerd(Cloudflare Workers ランタイム)と esbuild はプラットフォーム別バイナリの配置を
# postinstall で行うため、ビルドスクリプトの実行を明示的に許可する(pnpm 10 の既定は不許可)。
onlyBuiltDependencies:
  - esbuild
  - workerd
