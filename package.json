{
  "name": "kizami-monorepo",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=26"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "docs:dev": "pnpm --filter @kizami/docs dev",
    "docs:api": "node scripts/typedoc-validate.mjs",
    "docs:build": "pnpm docs:api && pnpm --filter @kizami/docs build"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
