{
  "name": "@kizami/mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "KIZAMI MCP サーバー — 公開打刻API(APIキー)の上に薄く実装した stdio MCP サーバー。DBには直接触らない",
  "scripts": {
    "start": "tsx src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "tsx": "^4.23.12"
  }
}
